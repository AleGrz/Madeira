diff --git a/dlls/win32u/dibdrv/bitblt.c b/dlls/win32u/dibdrv/bitblt.c
--- a/dlls/win32u/dibdrv/bitblt.c
+++ b/dlls/win32u/dibdrv/bitblt.c
@@ -1035,8 +1035,17 @@ static DWORD CDECL dibdrv_PutImage( PHYSDEV dev, HRGN clip, BITMAPINFO *info,
          *
          * One variable, one comparison, no runs wasted on either answer. */
         {
+            /* iOS-Madeira: three externs live in Winios.m and are only present
+             * in the iOS build. On the macOS host tree Wine also builds these
+             * .so's, and Mach-O linker semantics differ from ELF: plain
+             * __attribute__((weak)) marks a DEFINITION as coalescable, it does
+             * NOT create a weak reference to an undefined symbol -- the linker
+             * still errors on it. weak_import is the Darwin attribute that
+             * does what "weak reference, NULL if absent" means, so use it here.
+             * Wine mainline never sees this fork, so no non-Apple guard is
+             * needed. */
             extern void winios_dump_srcbits( const void *bits, int w, int h, int stride )
-                __attribute__((weak));
+                __attribute__((weak_import));
             if (winios_dump_srcbits)
                 winios_dump_srcbits( bits->ptr,
                                      dst->visrect.right - dst->visrect.left,
@@ -1050,7 +1059,7 @@ static DWORD CDECL dibdrv_PutImage( PHYSDEV dev, HRGN clip, BITMAPINFO *info,
          * buffer when the env var is unset. */
         extern void ios_srcwatch_arm_geom( const void *bits, unsigned long len,
                                            unsigned w, unsigned h, unsigned stride )
-            __attribute__((weak));
+            __attribute__((weak_import));
         if (ios_srcwatch_arm_geom)
         {
             unsigned gw = dst->visrect.right - dst->visrect.left;
@@ -1062,8 +1071,10 @@ static DWORD CDECL dibdrv_PutImage( PHYSDEV dev, HRGN clip, BITMAPINFO *info,
                                    gw, gh, gw * 4 );
         }
         else {
-        extern void ios_srcwatch_arm( const void *bits, unsigned long len );
-        ios_srcwatch_arm( bits->ptr,
+        extern void ios_srcwatch_arm( const void *bits, unsigned long len )
+            __attribute__((weak_import));
+        if (ios_srcwatch_arm)
+            ios_srcwatch_arm( bits->ptr,
                           (unsigned long)info->bmiHeader.biSizeImage ?
                               (unsigned long)info->bmiHeader.biSizeImage :
                               (unsigned long)((dst->visrect.right - dst->visrect.left) * 4 *
